# Replace these values with your real Terraform state bucket and lock table.
bucket         = "replace-with-terraform-state-bucket"
key            = "youtube-live-comment-filter/dev/terraform.tfstate"
region         = "ap-northeast-2"
encrypt        = true
dynamodb_table = "replace-with-terraform-lock-table"
